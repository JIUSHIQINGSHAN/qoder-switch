# 项目级 cargo 配置。
#
# 产物目录不再在这里钉死：GitHub Actions 的 runner 没有 E: 盘，钉在这里会让 CI 全挂。
# 本机的 C: 盘装不下 release target（约 7GB），由 scripts/build.sh 显式导出
# CARGO_TARGET_DIR=E:/qs-target 兜底；手动跑 cargo 时也请带上这个环境变量，
# 见 scripts/build.sh 与 README 的构建节。

[net]
git-fetch-with-cli = true
