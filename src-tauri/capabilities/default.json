{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口只拿到 core 与 opener：所有文件与进程操作都必须在 Rust command 里发生",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
