{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口只拿到 core 与 opener：所有文件与进程操作都必须在 Rust command 里发生。updater 仅为签名更新包的下载安装（tauri-plugin-updater 受管路径）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default"
  ]
}
